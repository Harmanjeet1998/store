<h1 class="title">Shopsoft</h1>
    <% @products.each do |product| %>
        <div class="heading" >
        <div class="imagediv"><%= image_tag(product.image_url)%></div>
	<div class="pname"> <%= product.name %></div>
        <div class="prise">₹<%= product.sp %></div>
        <div class="pcategory"><%= product.category %></div> 
        <!--<div><%= product.quantity %></div>-->
         <%= link_to 'Show', product %>
</div>
    <% end %>

<script>

$('.datatable').DataTable({
  dom: "<'row-fluid'<'span6'l><'span6'f>r>t<'row-fluid'<'span6'i><'span6'p>>",
  pagingType: "bootstrap"
});
</script>
